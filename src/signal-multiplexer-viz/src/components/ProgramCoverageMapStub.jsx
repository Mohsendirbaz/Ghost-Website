/**
 * Exhibition-build stub for ProgramCoverageMap.
 * The public exhibition build (VITE_EXHIBITION=1) aliases the Program Coverage
 * view to this stub so the internal coverage/gap-analysis dataset
 * (src/data/programCoverage.js) never enters the public bundle.
 */
export default function ProgramCoverageMapStub() {
  return null;
}
