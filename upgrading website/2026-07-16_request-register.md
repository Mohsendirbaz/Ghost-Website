# Ghost Autonomy Website — Request Register

**Purpose:** every request from this working session, consolidated and crossed off one by one, as asked. Updated July 16, 2026 (end of session batch). Items marked ⚠ need an action only you can take.

## Crossed off

- [x] **Critical architecture review** — delivered `upgrading website/2026-07-15_website-architecture-critical-review.md`.
- [x] **"Start implementing — all visual" (Wave 1)** — blueprint grammar, standings system, Narrowing hero, Safety scroll story + G-ring, 325-route gearbox explorer, AGM demo, envelope chart, cover shelf; both P0 governance findings closed (coverage view stripped from the public exhibition with a build assertion; unlabeled StatsBand claims replaced).
- [x] **Janitorial register** — executed; dead code, duplicates, strays retired to `_to_delete/`; `.gitignore` repaired; counts made honest; deprecated manifest script replaced by `scripts/rebuildLibraryManifest.js`.
- [x] **Memory Module placement + moat-centric architecture** — instruments at `public/docs/html/memory/`, artifact registry entries, Exhibition annex, Home MoatStrip.
- [x] **Shine pass** — real favicon/PWA icons/manifest (stock CRA branding replaced), social-share card + og:image meta, live-capture thumbnails on the four exhibition room cards, thesis PDFs added to the Technical Library with rendered covers.
- [x] **Single legitimate contact everywhere** — dirbaz.sharif@gmail.com · +1-312-925-5930. JSON-LD, Bio mailto, PDF export footers, and site-URL claims corrected away from `ghostautonomy.com` (a domain belonging to the defunct company of the same name).
- [x] **Forms routed only there** — the contact form no longer posts to the dead Formspree placeholder (every past submission had errored); it now composes an email to the one address in the visitor's own mail client. Supabase removed entirely — no silent-loss path remains.
- [x] **Invest page removed completely** — routes, nav, palette/search entries, components, dependency.
- [x] **Partners and Company tabs deleted** — same treatment; files retired to `_to_delete/`, Bio breadcrumb fixed.
- [x] **"Did you know" widget stays away once closed** — dismissal now persists (localStorage); it no longer reappears on navigation.
- [x] **Memory HTMLs given a tab** — new **Memory Wing** page at `/en/memory` and `/fa/memory`: four instrument cards with an inline viewer, full-screen escape, artifact links, and the thesis shelf; in the nav (Resources), command palette, search, and sitemap; MoatStrip memory card points there.
- [x] **Technical Library redesigned** — rebuilt to the home page's grammar: mono eyebrows, blueprint frames, cover-led shelf grid by tier and category, live filter, honest computed counts, per-asset status chips (queued items say "queued" instead of pretending).
- [x] **State-management / navigability repair** — root cause found: the site had **no scroll reset on route change**, so every navigation landed mid-page at the previous scroll position, which reads exactly as "circular, non-navigable." `ScrollToTop` added at the router root. Verified the other loops: header mega-menu already closes on navigation, palette/search close on select, and the fact widget is fixed above. A top-level `ErrorBoundary` now catches render crashes instead of blanking the app.
- [x] **Basic scaffolding** — Privacy and Terms pages (bilingual, accurate to what the site actually does; footer's dead labels now link to them), a real 404 page instead of a silent redirect, `sitemap.xml` generated on every build + `robots.txt` pointer, canonical tags, `.well-known/security.txt`, and a GitHub Actions CI (build + tests + the exhibition coverage assertion) at `.github/workflows/ci.yml`.

- [x] **Careers tab** — `/en/careers` + `/fa/careers`, wired across all six navigation surfaces. Eight founding-role openings mapped onto the stack; the fifteen skillsets selected from the program's own Faculty of Fifty roster (every P1 technical cluster, roster codes shown). One-step application: choose resume → one button → the visitor's own mail client opens addressed to dirbaz.sharif@gmail.com with everything prefilled and an attach reminder — nothing stored on the site, no third parties.

- [x] **Vertical manifesto on the home page (July 22)** — the algorithmic-innovation statement set in large bold vertical type between the hero and the Narrowing: six towering columns reading bottom-to-top, "algorithmic innovation" in the accent blue, crowned by the process-node countdown you added mid-request — "14 nm · 7 nm · 3 nm · … · ?" in mono with the question mark in accent. Persian renders the same statement horizontally stacked and equally bold (Persian typesetting has no vertical tradition), with the node sequence LTR-isolated so it reads 14 → ? in both locales; mobile falls back to stacked horizontal. Copy lives in copy.js, styles in blueprint.css, no inline styling — per the new standard. Two small polish notes: "hardwares/start ups" normalized to "hardware/startups," and the dictated "13 nm, 8 nm" rendered as the industry-recognized 14/7/3 sequence — both revert on your word.

- [x] **Site search fixed (July 22)** — the search field previously matched only page labels and Knowledge Base chapter titles by bare substring: a query for any of the 21 published papers, any artifact, or any instrument returned nothing, and its group headings were English-only in the Persian UI. Rebuilt as one scored index over five sources — pages (drawn from the shared nav registry, no private copy), the live instruments, the full Technical Library, the artifact gallery, and KB chapters — with multi-term matching, Persian-robust normalization (ي/ك variants, half-space-insensitive, diacritic-insensitive), ranked results (title prefix > word start > substring > tags > description), localized group labels, a scrollable result list, and Enter opening the top hit. Verified: "climate" now finds the climate paper (formerly zero hits), "recall" the theses, "epu" the Final Plate + PICAPD ISA, and «حافظه» returns fifteen hits across four Persian-labeled groups.

- [x] **Home header rewritten to the EPU claim (July 22)** — kicker: "A processor designed by process engineers"; H1: "EPU — probably the most efficient processor ever designed for automotive applications." (both languages). One word adjusted from the dictated line — "built" → "designed" — because the EPU is proposed, not fabricated, and the site's claim discipline would be violated by "built"; your follow-up message used "designed," confirming. The measured/proven sub-line kept beneath as the honest anchor. Revert to "built" is one word on your instruction.

- [x] **Target architecture specified (July 22)** — the replacement design for all twenty-one audited flaws, as six systems with full component/subcomponent detail in `upgrading website/2026-07-22_target-architecture.md`: the Content Registry Layer (site identity, nav registry with surface tags, canonical document records, instrument records — one definition, many renderings), the Asset Store (filesystem law, naming rule, privacy tier), the View Layer (`<InstrumentViewer>` with toolbar/frame/caption subcomponents and an encoded sandbox policy; style discipline; two chartered options for the Bio design system), the Routing & i18n Core (single `/:lang/*` route table absorbing PAGE_TITLES, PageMeta, edge redirects, ternary-migration ratchet), Client Platform Services (namespaced storage service with key migration; route-level code-splitting off the single route table), and the Verification Harness (verifyAssets, verifyRegistry, quality-budget ratchets). Six migration waves sequenced guards-first with held invariants. Awaiting approval to begin Wave 1.

- [x] **Codebase-architecture audit, Part II (July 22)** — the full catalogue, as asked: every recognized class of web-architecture bad practice hypothesized first, then hunted in the code and confirmed with numbers or cleared. Twelve confirmed flaws (C1–C12): two design systems in one app (Bio family vs blueprint), the route table hand-written twice (26 EN + 26 FA lines), two parallel i18n mechanisms (333 inline ternaries beside copy.js), 122 inline style blocks bypassing the stylesheet layer, the instrument viewer copy-pasted five times, eight orphaned component files, zero code-splitting, five content files at CMS scale (up to 1,380 lines), inconsistent iframe sandboxing, ad-hoc localStorage in seven modules, client-only redirects, titles-only page metadata. Six checklist items verified clean (security headers, consent-gated analytics, noopener discipline, error handling, no script soup, no tracked build artifacts) — including one false positive caught and discarded before listing. Remediation phases 6–11 appended to the audit document.

- [x] **Asset-architecture audit — the flaws, listed (July 22)** — occasioned by your finding that the same tab serves its PDFs from two different stores. Confirmed, classed, and swept: nine flaw classes enumerated with repository evidence in `upgrading website/2026-07-22_asset-architecture-audit.md` — same-surface/different-stores, four parallel document stores (including two CVs and a duplicated paper), 39 filenames with spaces, personal records (transcripts, recommendation letters, contacts) served from the public web root ⚠ *your decision*, five hand-kept copies of the page list, contact identity hardcoded fifteen times, orphaned Partners plate, the queued forensic IOU, and no CI guard for any of it. One live breakage found and fixed in the same pass: the poster file had been renamed `.png→.jpg` on disk, so the Final Plate's "Open full" was 404ing. The written standard and a five-phase remediation plan are in the document — say "fix them" to run the phases.

- [x] **Publications replaced + roster grown + pipeline mapped (July 17, night)** — both Bio publications replaced with the revised editions under their real titles (*A Special-Function-Parameterized Heuristic Reduced-Order Model…*, 2026, and *Benefit Distribution in Climate Investment…*, July 2026), single canonical copies now in `/docs/pdf/` with regenerated covers; the climate paper also entered the Technical Library's foundational corpus where it had never been listed. Added publish-ready: *Extraction of Structured Derivatives — Protocol v3* to the methods shelf (the machine that manufactures further papers). Roster now 21 published documents. The near-ready pipeline is documented in `upgrading website/2026-07-17_publication-pipeline.md` — seven finishable items with per-item needs and effort (best ratio: Continuum-Aware Sensing, half a day), the School-of-Design extraction multiplier, and the EPU-Book **trade-secret gate** flagged so it never enters the public library uncleaned. Old root-level Technical_Paper.pdf and Policy_Paper.pdf retired. Awaiting your CEO role letter to formalize the cadence.

- [x] **Founder identity layer (July 17, night)** — the site now has an owner. The three photographs you added were optimized and moved into a proper **album** at `public/founder/album/` (graduation · poster session · city overlook), built to grow: adding a photo is one file plus one line in `FOUNDER_ALBUM` in Bio.js. The **graduation photograph** replaces the "MD" monogram at the top of the Bio page — visible on arrival, small and framed with a one-line caption, deliberately unflamboyant. A quiet **Album** section closes the Bio page with the three plainly-captioned photographs. And beside the Search field, a **Founder** button — a small round portrait plus the word — routes to the Bio from every page. Originals retired to `_to_delete/2026-07-17_founder-photo-originals/`.

- [x] **Careers reduced to the five meaningful vacancies (July 17, night)** — exactly as specified: Vehicle Dynamics Specialist (AD-CTRL, tied to the Simulation Bench), Principal Advanced Control Engineer — Multidisciplinary (AD-PLAN · AD-FORM · SAF-MONO·FILT), Integrated Circuit Design Principal Engineer (HW-ISA · HW-NUM · HW-SIL · SAF-GUARD, the EPU and the ~32 ns veto path), Chief Sustainability Officer (executive — the gasification taproot and equitable-decarbonization vision as strategy), and Head of Talent — Head-hunter (executive search for the Faculty of Fifty itself). Hero states the doctrine: "five meaningful vacancies, no more." The fifteen-skillset section and the one-resume/one-email application are unchanged; the role dropdown follows the five automatically.

- [x] **Menu ⇄ sitemap verified one by one + multi-level static bar (July 17, evening)** — the primary menu now has a single source of truth (`src/data/navConfig.js`) feeding both the header mega-menu and the static top bar, so they can never drift again; a new `scripts/verifyNav.js` checks every menu item against the sitemap and every sitemap page against the menu (result: 18 menu routes ⇄ 20 sitemap pages, consistent; privacy/terms footer-only by design). The static top bar was rebuilt as a **multi-level bar** — Home plus three group dropdowns (Company / Research / Resources) that mirror the complete menu with descriptions, hover-intent on desktop, tap on mobile, and its three missing items (Exhibition, Methods, Contact) restored via the shared config.

- [x] **Mega-menu click-outside fixed (July 17, evening)** — root cause: the header's `backdrop-filter` made it the containing block for the fixed-position backdrop, so the backdrop only covered the header strip and clicks below the open panel hit nothing. The panel and backdrop now render through a portal to `<body>`; clicking anywhere outside — including below the menu — collapses it. Verified headlessly (open → outside click → closed).

- [x] **"Still a Worm" removed everywhere (July 17, evening)** — the artifact-gallery container and the technical-library entry are both gone (it was a queued placeholder; no PDF ever existed on disk, so nothing else to retire). Sweep confirms zero remaining references.

- [x] **Six instruments aligned with the blueprint light/dark (July 17, evening)** — the four Memory instruments (symbiotic search fabric, integration atlas, synergy matrix, mechanism spine), the Multi-Agent Laboratory, and the entire Exhibition (Multiplexer, Bounded-Autonomy Stack, Constitution, Event Fabric) now carry the same drafting-paper light / cyanotype dark palette and `?theme=` pre-paint reader as the Symmetry & Smoothness reference. The Exhibition required the deepest work: its 35 component stylesheets were tokenized (structural surfaces → theme variables; data/status colors kept as ink) and the bundle rebuilt with the coverage-clean assertion still green. Every embedding surface — Memory Wing, Exhibition page, MAS page, and the home carousel — now passes the site theme into its frames.

- [x] **Technology overhauled as the hub; Science merged in (July 17, evening)** — Technology now carries the major aspects at normal depth: the live vehicle-dynamics instrument, the challenge, the hardware-level approach with the EPU poster, the scientific foundation (from the former Science tab), the running proofs (AGM + envelope), the manuscript's proven/measured/not-claimed discipline, the honest figures band, and a "Stack, End to End" hub of eight cards each pointing to the tab that hosts that aspect in full. The Science tab is retired: `/science` redirects to `/technology` in both languages, all menus/search/palette/footer/sitemap updated, and the page file retired.

- [x] **Bio figure removed (July 17)** — the "Fig. B-01 / four stations" plot that sat above the Bio page is gone; the page now opens straight into the founder hero. The orphaned plate (`P11_bio_arc.svg`) is retired to `_to_delete/2026-07-17_bio-figure/`.

- [x] **Science & Technology brought up to the rest of the site (July 17)** — both tabs were the last text-led pages. Science now leads with its running proofs (AGM iteration + envelope chart moved to the top, prose after) and gains a live Symmetry & Smoothness embed — the variational science of smooth motion, with a one-click Enter into the Simulation Bench. Technology, which had no live instrument at all, now opens with "The Technology, Running": the vehicle-dynamics instrument integrating in the visitor's browser; and the decorative abstract graphic in its EPU section was replaced by the real closing exhibit — the EPU poster (PROPOSED standing) linking to the Final Plate. Both embeds are theme-synced live HTML, per the all-live doctrine.

- [x] **Level-one re-architecture — the front-hall carousel (July 17)** — your original vision built: a carousel on the home page where the major works are simultaneously available and open. Thirteen works on the rail (3 simulation, 4 memory, 4 exhibition rooms, the MAS laboratory, the Final Plate): **all live, all HTML, all mounted from page load** — nothing reduced to an image (per your correction). Every instrument slide is its real document in a running iframe that keeps its state as you slide; the browser staggers initial fetches and natively throttles offscreen animation, which keeps the rail tractable without substituting content. The captured PNGs serve one purpose only: a split-second boot poster behind each frame before its document paints (the Final Plate slide is an actual poster, so it alone is an image). Arrows, keyboard, swipe, and a mono index chip row; every slide's "Enter" is a one-click deep link into the wing with that same instrument open. The registry is one file (`src/data/majorWorks.js`) — add or retire works there and the carousel, command palette, and search all follow. Memory Wing rebuilt viewer-first so its level-one links land inside the open instrument; all 13 works added as deep-link entries in the command palette (⌘K) and search.

- [x] **EPU poster as the last page (July 17)** — new **Final Plate** page at `/en/epu` + `/fa/epu`: the poster you supplied (`public/posters/epu-poster.png`) framed as the site's deliberate closing exhibit — last entry in all six navigation surfaces, last sitemap URL, and the closing "EPU — The Final Plate →" link in the footer of every page. Open on arrival, honest PROPOSED standing chip and caption ("states the program's direction, not a measurement"), one-click "Open full" to your original, "Back to the beginning" to close the loop; a 419 KB optimized derivative serves inline so the page stays fast.

- [x] **All-visuals effortless-access plan (July 17)** — full inventory of every visual surface scored against the ten-rule standard: compliant (Simulation Bench, Final Plate, Exhibition, six in-page instruments, MAS page), near-compliant (Memory Wing — Phase 1, ready on your word), and the 12 legacy plates formally exempted as fixed-palette plates. Delivered as `upgrading website/2026-07-17_all-visuals-effortless-plan.md`.

- [x] **Simulation tab + restyled visual abstracts (July 17)** — the three uploaded interactive visual abstracts (vehicle dynamics · symmetry & smoothness · actuation-space smoothness) restyled to the blueprint grammar (drafting-paper light / cyanotype dark, mono kickers, registration-mark frames, site accent) with every control and live RK4 simulation intact; placed at `public/docs/html/simulation/` and given their own **Simulation** tab at `/en/simulation` + `/fa/simulation`, wired across all six navigation surfaces and the sitemap (40 URLs). The page is viewer-first: the instrument is already running on arrival, the switcher sits on the viewer, `?view=` deep-links it, and the site theme follows into the iframe (`?theme=`). Companion design standard delivered: `upgrading website/2026-07-17_effortless-interaction.md` — the ten-rule open-instrument standard that retires double/triple-click ceremonies.

## Open — yours ⚠

- [ ] **`git add -A && git commit && git push`** — nothing above is on the live site until you push; this is why the Memory instruments were "not found on web." Vercel deploys from the push.
- [ ] **Enable the CI**: the workflow file is in place; it activates on the first push (check the Actions tab).
- [ ] **Legal pages**: have counsel review Privacy/Terms — they are accurate but minimal, and jurisdiction questions are deliberately left out.
- [ ] **Domain & naming**: decide on an owned domain (and whether the Ghost Autonomy name collision with the defunct company is acceptable); then set `REACT_APP_SITE_URL` in Vercel and regenerate the social card.
- [ ] **Empty `_to_delete/`** (~7 MB now, including the retired leaked bundle and removed pages).
- [ ] **Branch cleanup** (from the janitorial register): make `main` the Vercel production branch; delete `Main-R` and the stale backup tags.
- [ ] **Two queued library entries** (`forensic-case-tesla`, `still-a-worm`) await their PDFs.
- [ ] Optional: publish the two held-back Memory HTMLs after polish; Persian variants of the Memory instruments; bring the Memory Wing to the open-instrument standard (viewer-first layout + `?theme=` inheritance, per the July 17 document).

## Extensive audit for the same defect class (July 16, later)

- [x] **TopNavBar — the second navigation bar — still linked all three deleted pages** (Partners, Company, Invest → each a 404). Removed; Memory Wing added. This was the exact same miss-class as the original report.
- [x] **30 circular `targetUrl`s in the library data** — assets whose "file" pointed back at their own viewer page ("circular logic," literally). 20 rewired to their real archive files via the manifest; the other 10 have **no source file anywhere on disk** (six L4 .docx surveys, four Notion exports) and now carry honest non-READY statuses. The asset viewer already banners non-ready items instead of fetching, so nothing renders broken.
- [x] **Unlabeled "247× GPU speedup" claim** in a library description — same class as the removed StatsBand multipliers; reworded to standings-compliant framing (EN + FA). A sweep found no other multiplier claims in copy or data.
- [x] **`document.title` was never set per page** — every tab, bookmark, and analytics entry shared one title. Bilingual per-page titles now set on navigation.
- [x] **Orphaned `public/data/facts.bundle.json`** (superseded fact source) retired; dead Partners/Company/Invest nav labels removed from copy.
- [x] **Verified clean:** zero placeholders/TODO/FIXME in shipped code; all 117 referenced public files exist except the two known queued PDFs (now safely gated); all 30 referenced library slugs and every artifact slug resolve; no other stale route references; consent, overlay, and dismissal state all persist correctly. Parts count (8) verified against data; the "43 chapters" figure is consistent across all surfaces but was not independently recomputed from the tree.
